// Undergrad-level explanations for each component of the Kimi K3 (2.8T) architecture.
// Diagram reference: Sebastian Raschka’s Kimi K3 architecture figure.

export const COMPONENTS = {
  /* ------------------------- Main pipeline ------------------------- */
  'tokenized-text': {
    name: 'Tokenized Text',
    short: 'The raw input: text chopped into integer token IDs.',
    what: `Before a language model can process text, the text is split into “tokens” — small chunks like words, sub-words, or characters. Each token is mapped to an integer ID from a fixed vocabulary (often ~100k–160k entries).`,
    why: `Neural networks operate on numbers, not strings. Tokenization is the bridge: it converts arbitrary text into a sequence of integers the model can look up and process. Sub-word tokenization (like BPE) balances vocabulary size against sequence length — common words get one token, rare words get split into pieces.`,
    intuition: `Think of it like a dictionary lookup in reverse: “The cat sat” might become [791, 8415, 7731]. The model never sees letters — only these IDs.`,
  },
  'token-embedding': {
    name: 'Token Embedding Layer',
    short: 'Turns each token ID into a dense vector of numbers.',
    what: `A big lookup table (a matrix of size vocabulary × hidden_dim). Each token ID selects one row — a vector of a few thousand floating-point numbers that represents that token.`,
    why: `Integer IDs carry no meaning by themselves (token 791 isn’t “close to” token 792). Embeddings place tokens in a continuous vector space where similar meanings can end up geometrically close. These vectors are learned during training, so the model discovers useful representations on its own.`,
    intuition: `Imagine every word in the vocabulary gets a point in a huge multi-dimensional space. During training, “cat” and “kitten” drift toward each other because they appear in similar contexts. In Kimi K3, this embedding also feeds the “Depth sources” path — the original token embedding is re-injected into deeper layers so it’s never forgotten.`,
  },
  'transformer-stack': {
    name: '93× Transformer Layers',
    short: 'The core of the model: 93 stacked blocks, each with attention + FFN/MoE.',
    what: `The gray/green region repeats 93 times. Layer 1 uses a dense feed-forward network; layers 2–93 use LatentMoE (Mixture-of-Experts). The layers are organized into 8 “AttnRes blocks”: 7 blocks of 12 layers each, plus one 9-layer block.`,
    why: `Depth is how transformers build increasingly abstract representations: early layers handle syntax and local patterns, deeper layers handle semantics, reasoning, and long-range structure. Grouping layers into blocks lets Kimi K3 apply its special “AttnRes” residual scheme (the w/α weighting) at block boundaries, which helps signals survive across a very deep network.`,
    intuition: `Each layer refines the representation a little: layer by layer, “raw tokens” become “meaningful context”. 93 layers is very deep — the block-level residuals act like express lanes so information from the bottom can still reach the top intact.`,
  },
  'attn-sublayer': {
    name: 'Attention Sublayer',
    short: 'Where tokens look at each other and exchange information.',
    what: `The first half of each transformer layer. It normalizes the input (RMSNorm 1), runs an attention mechanism (Kimi Delta Attention or Multi-head Latent Attention depending on the layer), and adds the result back to the input stream via the “current group sum”.`,
    why: `Attention is the only place where tokens communicate. Everything else (embeddings, FFN) processes each token independently. Without attention, the model couldn’t understand that “it” in a sentence refers to “the cat” three words earlier.`,
    intuition: `Picture each token asking every previous token: “are you relevant to me?” and then blending in the answers, weighted by relevance. That weighted blend is the attention output.`,
  },
  'rmsnorm-1': {
    name: 'RMSNorm 1 (pre-attention)',
    short: 'Rescales activations to a stable size before attention.',
    what: `Root-Mean-Square Normalization: divides each token’s vector by its RMS (a measure of its magnitude), then multiplies by a learned scale. Unlike LayerNorm, it skips mean subtraction — simpler and slightly faster.`,
    why: `Deep networks are numerically fragile: activations can explode or shrink as they pass through many layers. Normalizing before each sublayer (called “pre-norm”) keeps every layer’s input in a predictable range, which makes training a 93-layer, 2.8-trillion-parameter model stable.`,
    intuition: `Like normalizing the volume of an audio signal before feeding it to the next effect pedal — the pedal always receives a signal in the range it was designed for.`,
  },
  'kda-mla-block': {
    name: 'Kimi Delta Attn / Multi-head Latent Attn',
    short: 'The attention mechanism — KDA in most layers, MLA every 4th layer (3:1 ratio).',
    what: `Kimi K3 alternates two attention types: Kimi Delta Attention (KDA), a linear-attention variant that is cheap for long sequences, and Gated Multi-head Latent Attention (MLA), a full attention with compressed KV cache. Pattern: layers 1–3 use KDA, layer 4 uses MLA, and so on (3:1 ratio).`,
    why: `Full attention costs O(n²) in sequence length — prohibitive at 1M-token context. KDA runs in linear time but is less precise at exact long-range retrieval. Mixing them gets the best of both: KDA handles most of the workload cheaply, while periodic MLA layers give the model precise global lookups. Click the insets on the right to explore each in detail.`,
    intuition: `Like reading a book: most of the time you skim with a running mental summary (KDA), but every so often you stop and flip back to check an exact quote (MLA).`,
  },
  'nope': {
    name: 'NoPE (No Positional Encoding)',
    short: 'The MLA layers use no explicit positional encoding.',
    what: `“NoPE” means the full-attention (MLA) layers do not add positional encodings such as RoPE. Word-order information reaches them indirectly, through the KDA layers, which are inherently order-sensitive.`,
    why: `Positional encodings like RoPE are typically trained at a fixed context length and degrade when you extrapolate beyond it. By making the global-attention layers position-agnostic, Kimi K3 generalizes more gracefully to very long contexts (1M tokens). KDA processes tokens sequentially — like an RNN — so position is baked into its recurrence for free.`,
    intuition: `If your note-taking (KDA) already records the order of events, the person doing the global lookup (MLA) doesn’t need timestamps on every page — the ordering is already encoded in the notes.`,
  },
  'current-group-sum': {
    name: 'Residual Add (current group sum)',
    short: 'Adds the attention output back onto the running signal.',
    what: `A simple elementwise addition: output = input + attention_result. The “current group sum” is the running residual stream accumulated within the current AttnRes block.`,
    why: `Residual connections are the single most important trick for training deep networks. Each sublayer only needs to learn a small correction (“delta”) on top of the identity, and gradients can flow backwards through the addition unimpeded — no vanishing gradients even at 93 layers.`,
    intuition: `Think of the residual stream as a shared document that flows upward through the network. Each sublayer reads it and appends its notes, rather than rewriting the whole document from scratch.`,
  },
  'ffn-sublayer': {
    name: 'FFN / MoE Sublayer',
    short: 'Per-token processing: where most of the “knowledge” lives.',
    what: `The second half of each layer: RMSNorm 2 followed by either a dense feed-forward network (layer 1) or a LatentMoE (layers 2–93), then another residual add.`,
    why: `Attention moves information between tokens; the FFN transforms it within each token. Research suggests FFN layers act as key-value memories storing the model’s factual knowledge. That’s why they hold most of the parameters — and why making them sparse (MoE) is the key to scaling to 2.8T parameters.`,
    intuition: `If attention is tokens “talking to each other”, the FFN is each token “thinking privately” about what it just heard, drawing on the model’s stored knowledge.`,
  },
  'rmsnorm-2': {
    name: 'RMSNorm 2 (pre-FFN)',
    short: 'Same normalization trick, applied before the FFN/MoE.',
    what: `Identical mechanism to RMSNorm 1: rescale the vector to unit RMS, then apply a learned per-dimension scale.`,
    why: `Every sublayer gets its own normalization, because the residual stream’s magnitude grows as layers keep adding to it. Fresh normalization before the FFN guarantees the FFN sees well-behaved inputs regardless of how large the accumulated residual has become.`,
    intuition: `Same audio-volume analogy as RMSNorm 1 — each effect pedal (sublayer) gets its own input level control.`,
  },
  'ffn-latentmoe': {
    name: 'FFN / LatentMoE',
    short: 'Mixture-of-Experts: 2.8T total parameters, but only a fraction active per token.',
    what: `Instead of one big feed-forward network, an MoE layer holds many “expert” FFNs plus a small router. For each token, the router picks the top-k most relevant experts, and only those run. “Latent” MoE adds a compression step so expert weights are stored/loaded more efficiently. Layer 1 stays dense (a normal FFN) for stability.`,
    why: `This is how you get a 2.8-trillion-parameter model that’s affordable to run: total capacity is enormous, but per-token compute only touches a few experts. Different experts specialize (e.g., code, math, multilingual text), so capacity is used where it matters. A dense first layer avoids routing instability on raw embeddings.`,
    intuition: `Like a hospital: instead of every doctor examining every patient, a triage desk (the router) sends each patient (token) to the 2–3 relevant specialists (experts). Huge total staff, small per-patient cost.`,
  },
  'updated-group-sum': {
    name: 'Residual Add (updated group sum)',
    short: 'Adds the FFN/MoE output back — completing one layer.',
    what: `The second residual addition in the layer: it adds the FFN/MoE output to the stream, producing the “updated group sum” that flows into the next layer (or the next AttnRes block boundary).`,
    why: `Same reason as the first residual add: identity shortcut for stable gradients. Tracking it as a “group sum” matters for Kimi K3's AttnRes scheme — at block boundaries, this accumulated sum is combined with depth sources using the learned w/α weights.`,
    intuition: `The shared document gets one more append — now containing this layer’s full contribution — and is passed up to the next layer.`,
  },
  'w-alpha': {
    name: 'w / α Weights (AttnRes)',
    short: 'Learned gates that mix depth sources into the residual stream.',
    what: `Small learned scalar weights (w and α, shown as pink circles) that control how much of the “depth sources” — the original token embedding and summaries of earlier blocks — gets blended into each block’s residual stream, alongside the block’s own computation.`,
    why: `In very deep networks, the original input signal can get diluted after dozens of layers. AttnRes (“Attention over Residuals”) lets each block adaptively re-inject fresh copies of the embedding and earlier block outputs. Making the weights learnable means the model itself decides how much “memory of the past” each depth needs.`,
    intuition: `Like a chef tasting a long-simmering sauce and deciding to add a splash of the original stock to bring back flavors that cooked away. The w/α values are how big a splash each layer takes.`,
  },
  'depth-sources': {
    name: 'Depth Sources',
    short: 'A shortcut bundle: the embedding + earlier block summaries, available to all depths.',
    what: `A collection of signals kept available throughout the network: the original token embedding and summaries of earlier AttnRes blocks. These are routed (pink arrows) to the w/α gates of later blocks and to the Final Block AttnRes.`,
    why: `Standard residual connections only link adjacent layers; information from layer 5 reaching layer 90 has to survive 85 additive updates. Depth sources create direct highways from early representations to deep layers, fighting signal degradation in a 93-layer network — similar in spirit to DenseNet connections in CNNs.`,
    intuition: `Like a long relay race where, instead of only passing the baton runner-to-runner, every runner can also radio back to the start and to earlier checkpoints for the original instructions.`,
  },
  'depth-embedding': {
    name: 'Depth Source: Embedding',
    short: 'The raw token embedding, re-injectable at any depth.',
    what: `A direct copy of the token embedding layer’s output, kept accessible to all AttnRes blocks via the w/α gates.`,
    why: `After 90+ layers of processing, the model’s representation is highly abstract — sometimes it needs to look back at what the literal input token was (e.g., for copying, spelling, or exact repetition tasks). This shortcut makes the raw token identity available everywhere at almost no cost.`,
    intuition: `Like keeping the original photo next to your heavily-edited version, so you can always check what you started from.`,
  },
  'depth-earlier-blocks': {
    name: 'Depth Source: Earlier Block Summaries',
    short: 'Outputs of previous AttnRes blocks, available to later blocks.',
    what: `Summaries (accumulated group sums) of earlier AttnRes blocks, made available to later blocks through the same w/α gating mechanism.`,
    why: `Different depths capture different levels of abstraction — early blocks hold more syntactic/literal features, later blocks more semantic ones. Letting deep blocks directly access intermediate representations (not just their immediate predecessor) gives the model a richer palette and improves gradient flow during training.`,
    intuition: `When writing chapter 10 of a book, it helps to have your notes from chapters 2 and 5 open on the desk — not just chapter 9.`,
  },
  'final-block-attnres': {
    name: 'Final Block AttnRes',
    short: 'One last learned mix of all depth sources before the output.',
    what: `A final application of the AttnRes mechanism: it combines the last block’s output with the depth sources (embedding + earlier block summaries) using learned weights, producing the vector that goes into the final normalization.`,
    why: `The best representation for predicting the next token isn’t necessarily just the deepest layer’s output — sometimes shallower features matter. This final mixing stage lets the model compose its ultimate answer from all depths of the network.`,
    intuition: `Like a film editor’s final cut: rather than only using the last take, they splice together the best material from every take recorded along the way.`,
  },
  'final-rmsnorm': {
    name: 'Final RMSNorm',
    short: 'One last normalization before computing output scores.',
    what: `The same RMSNorm operation, applied once more to the final combined representation before the output projection.`,
    why: `After 93 layers of residual additions plus the final AttnRes mix, activation magnitudes vary. The output layer computes similarity scores against every vocabulary embedding — normalizing first ensures these scores are well-calibrated and the softmax over 100k+ tokens behaves sensibly.`,
    intuition: `A final quality-control check that the signal is at standard level before it hits the “speaker” (the output layer).`,
  },
  'linear-output': {
    name: 'Linear Output Layer',
    short: 'Maps the final vector to a score for every vocabulary token.',
    what: `A single matrix multiplication of size hidden_dim × vocab_size (also called the “unembedding” or LM head). It produces one logit (raw score) per vocabulary token; a softmax converts these to next-token probabilities.`,
    why: `This is where the model’s internal representation is translated back into language: everything the 93 layers computed is compressed into “how likely is each possible next token?”. Generation then samples from this distribution, appends the chosen token, and the whole process repeats.`,
    intuition: `The inverse of the embedding layer: embeddings go words → vectors; this layer goes vector → scores over words. The token with a high score is what the model “wants to say” next.`,
  },
  'context-length': {
    name: '1M Token Context',
    short: 'Kimi K3 can attend over sequences of up to 1 million tokens.',
    what: `The context length is the maximum number of tokens the model can process in one pass — here, one million (roughly 8 full-length novels).`,
    why: `Long context enables use cases like analyzing entire codebases, books, or long legal documents in one shot. It’s feasible here because of the architecture: KDA scales linearly with sequence length, MLA compresses the KV cache, and NoPE avoids position-encoding breakdown at extreme lengths. With standard full attention, 1M tokens would need ~10¹² attention score computations per layer — completely impractical.`,
    intuition: `Context is the model’s working memory. 1M tokens means it can “hold in mind” an entire codebase while answering a question about one function.`,
  },
  'layer-ratio': {
    name: '3:1 KDA-to-MLA Ratio',
    short: 'Three cheap linear-attention layers for every one full-attention layer.',
    what: `The layer schedule: layers 1, 2, 3 use Kimi Delta Attention, layer 4 uses Gated MLA, then the pattern repeats. Every layer is followed by its MoE sublayer.`,
    why: `It’s a compute-quality trade-off, found empirically. Full attention everywhere is too expensive at 1M context; linear attention everywhere loses precise long-range retrieval (e.g., “find the exact phone number mentioned 500 pages ago”). A sparse sprinkling of MLA layers restores retrieval ability at a fraction of the cost.`,
    intuition: `A security team where 3 out of 4 guards do quick continuous patrols (KDA) and the 4th periodically reviews all camera footage in detail (MLA).`,
  },

  /* ------------------------- MLA inset ------------------------- */
  'mla-overview': {
    name: 'Gated Multi-head Latent Attention (MLA)',
    short: 'Full attention with a compressed KV cache and an output gate.',
    what: `MLA (introduced in DeepSeek-V2) is standard multi-head attention with one key change: keys and values are not cached directly. Instead, the input is compressed into a small “latent” vector, which is cached, and K/V are reconstructed from it on the fly. Kimi K3 adds a sigmoid gate that modulates the output.`,
    why: `During generation, the KV cache dominates memory: for every past token you’d normally store full keys and values for every head. Caching one small latent vector per token instead shrinks this by a large factor — essential at 1M context. Surprisingly, this compression barely hurts quality, and the gate adds per-token control over how much attention output flows through.`,
    intuition: `Instead of keeping verbatim transcripts of every past conversation (full KV cache), you keep compact meeting minutes (the latent) and re-expand them into full detail only when needed.`,
  },
  'mla-latent': {
    name: 'Latent Vector (KV compression)',
    short: 'The small compressed vector that replaces the full KV cache.',
    what: `A down-projection of each token’s hidden state into a much lower-dimensional vector. This latent is what gets stored in the cache; separate up-projection matrices reconstruct keys and values from it when attention is computed.`,
    why: `This is the whole point of MLA. If keys and values across all heads live in a shared low-dimensional subspace, you only need to store the subspace coordinates. Memory savings scale directly with the compression ratio — the difference between fitting a 1M-token cache on your GPUs or not.`,
    intuition: `Like storing photos as compressed JPEGs instead of RAW files: massively smaller storage, decode on demand, and the quality loss is barely noticeable for the task at hand.`,
  },
  'mla-qkv': {
    name: 'Q, K, V Projections',
    short: 'Three linear maps producing queries, keys, and values.',
    what: `Linear layers producing the three roles in attention: the Query (“what am I looking for?”), the Key (“what do I contain?”), and the Value (“what information do I carry?”). In MLA, K and V are produced from the compressed latent; Q comes from its own projection of the input.`,
    why: `Attention scores are dot products between queries and keys: token i attends strongly to token j when Q_i · K_j is large. The output is then the average of the Values, weighted by these scores. Separating the three roles lets “what matches” be decoupled from “what gets communicated”.`,
    intuition: `A library search: your query is the search phrase, each book’s key is its index entry, and the value is the book’s actual content. Matching happens between query and keys; what you take home is the values.`,
  },
  'mla-qknorm': {
    name: 'QK-Norm',
    short: 'Normalizes queries and keys before they meet.',
    what: `RMSNorm applied to queries and keys individually before the dot-product attention is computed.`,
    why: `If Q or K vectors grow too large during training, attention logits explode, softmax saturates into near one-hot distributions, and gradients vanish — a well-known instability in large-model training. QK-Norm bounds the logits and keeps huge training runs stable.`,
    intuition: `Comparing two vectors by direction rather than length — like judging whether two people point at the same star, regardless of how long their arms are.`,
  },
  'mla-gate': {
    name: 'Sigmoid Gate',
    short: 'A learned per-token valve on the attention output.',
    what: `A parallel linear layer computes a gating signal from the input, squashed by a sigmoid into (0, 1), and elementwise-multiplied (⊗) with the attention output.`,
    why: `Not every token benefits from attention at every layer — sometimes the best move is “change nothing”. The gate lets the model dampen or pass the attention output per token per dimension. Gating also helps with the “attention sink” problem, giving the model a graceful way to output nothing instead of dumping attention on irrelevant tokens.`,
    intuition: `A volume knob controlled by the model itself: for each token, it decides how loudly the attention result should be mixed into the residual stream.`,
  },
  'mla-out': {
    name: 'Output Projection',
    short: 'Merges all attention heads back into one vector.',
    what: `A final linear layer that takes the concatenated outputs of all attention heads (after gating) and projects them back to the model’s hidden dimension.`,
    why: `Each head attends to different things (one might track syntax, another coreference). The output projection learns how to combine these parallel perspectives into a single coherent update for the residual stream.`,
    intuition: `Several specialists each write a paragraph; the output projection is the editor who merges them into one coherent memo.`,
  },

  /* ------------------------- KDA inset ------------------------- */
  'kda-overview': {
    name: 'Kimi Delta Attention (KDA)',
    short: 'Linear attention: a fixed-size memory updated with a “delta rule”, O(n) in sequence length.',
    what: `KDA replaces the quadratic attention computation with a recurrent one: it maintains a fixed-size memory matrix that is updated token by token using a gated delta rule. Reading from memory is a simple matrix-vector product — cost per token is constant, independent of how long the sequence is.`,
    why: `Full attention at 1M tokens is prohibitively expensive in both compute (O(n²)) and memory (the KV cache grows with n). KDA’s memory is fixed-size, so cost stays flat no matter how long the context gets. It descends from a line of work on linear attention (DeltaNet, Gated DeltaNet, Mamba-style gating).`,
    intuition: `Instead of re-reading the entire conversation for every new word (full attention), KDA keeps a whiteboard of fixed size, erasing and rewriting entries as new information arrives.`,
  },
  'kda-delta-rule': {
    name: 'Gated Delta Rule',
    short: 'The memory update: erase the old value for a key, write the new one — with learned gates.',
    what: `For each new token, the rule first reads what the memory currently stores for this token’s key, computes the difference (“delta”) from the new value, and writes only that correction — scaled by a learning-rate-like gate β. A decay gate α simultaneously fades old memories. In Kimi’s version, the decay is fine-grained: each feature channel can forget at its own rate.`,
    why: `Naive linear attention only adds to memory, so it fills up and gets blurry. The delta rule makes memory overwritable: storing a new value for an existing key replaces the old one instead of piling on top. Gates α and β let the model control, per token, what to forget and how strongly to write — crucial for staying sharp over a million tokens.`,
    intuition: `A contact list rather than a diary: when a friend changes their number, you overwrite the old entry (delta rule) instead of appending a new line, and stale contacts gradually fade out (decay gate α).`,
  },
  'kda-l2norm': {
    name: 'L2 Norm (on Q and K)',
    short: 'Forces queries and keys to unit length.',
    what: `Queries and keys are divided by their Euclidean norm, making them unit vectors before entering the delta rule.`,
    why: `The delta rule’s stability depends on the sizes of keys: if key magnitudes vary wildly, memory updates can explode or become negligible. Unit-normalizing makes every memory read/write well-conditioned, so the recurrence stays stable across millions of update steps.`,
    intuition: `Making sure every entry written on the whiteboard uses the same size handwriting — nothing gets written so large it covers everything else.`,
  },
  'kda-conv': {
    name: 'Short Convolution',
    short: 'A small depthwise convolution mixing a few neighboring tokens.',
    what: `A short (e.g., width-4) depthwise causal convolution applied to Q, K, and V before the recurrence. Each position gets a quick blend of itself and the few tokens immediately before it.`,
    why: `Recurrent/linear attention processes strictly one token at a time and can struggle with local patterns that span a few adjacent tokens (like multi-token words or short phrases). A cheap convolution handles this local mixing directly, freeing the recurrent memory to focus on long-range content. This trick is standard in modern recurrent LLM blocks (Mamba, GLA, etc.).`,
    intuition: `Skimming a couple of words back as you read each new word — cheap local context, before committing anything to long-term memory.`,
  },
  'kda-silu': {
    name: 'SiLU Activation',
    short: 'A smooth nonlinearity applied to the value path.',
    what: `SiLU (also called Swish): f(x) = x · sigmoid(x). A smooth curve that’s roughly linear for large positive inputs and smoothly gates off negative ones.`,
    why: `Nonlinearities give networks their expressive power — without them, stacked linear layers collapse into a single linear map. SiLU in particular is smooth everywhere (nice gradients) and consistently outperforms ReLU in transformer-scale models, which is why it’s the default choice in modern LLMs.`,
    intuition: `A soft valve: strongly positive signals pass through untouched, negative ones are squeezed toward zero — but gently, with no hard corner like ReLU’s.`,
  },
  'kda-alpha-beta': {
    name: 'α and β Gates',
    short: 'Forget-rate (α) and write-strength (β), computed per token.',
    what: `Two gating signals computed from the input by small linear layers: α controls how quickly existing memory decays (fine-grained — per channel in KDA), and β controls how strongly the current token’s correction is written into memory.`,
    why: `Fixed forgetting rates can’t fit all situations: sometimes context from long ago is crucial (keep α high), sometimes the topic has shifted and old memory is noise (drop α). Making both data-dependent lets the model dynamically manage its limited memory — this gating is a large part of why modern linear attention works as well as it does.`,
    intuition: `α is the eraser strength and β the pen pressure on the memory whiteboard — and the model adjusts both for every single token it reads.`,
  },
  'kda-gate': {
    name: 'Sigmoid Output Gate',
    short: 'Same idea as MLA\u2019s gate: a valve on the block output.',
    what: `A parallel linear projection of the input, passed through a sigmoid, that elementwise-multiplies (⊗) the memory-read output before the final projection.`,
    why: `It lets each token modulate how much of the recurrent memory’s output actually enters the residual stream. If the memory read is irrelevant for this token, the gate can shut it off rather than injecting noise.`,
    intuition: `The final mixing-desk fader before this block’s signal joins the main mix.`,
  },
  'kda-rmsnorm': {
    name: 'Output RMSNorm',
    short: 'Normalizes the memory-read output before gating and projection.',
    what: `An RMSNorm applied to the output of the gated delta rule, before the sigmoid gate and final linear projection.`,
    why: `The recurrent memory’s output magnitude can drift depending on how much has been written into memory. Normalizing here keeps the block’s output scale consistent with the rest of the network, protecting training stability.`,
    intuition: `Same volume-leveling as elsewhere — every path’s signal is brought to standard level before rejoining the main stream.`,
  },
  'kda-in': {
    name: 'Input Projections (Q, K, V)',
    short: 'Linear layers producing queries, keys, and values from the token stream.',
    what: `Standard linear projections that map each token’s hidden vector into the query, key, and value spaces used by the delta-rule memory (plus the projections that produce the α, β, and gate signals).`,
    why: `Just like in regular attention, the model needs separate learned “views” of each token: what to look up (Q), how to index memory (K), and what content to store (V). Learning these projections is what lets KDA decide which aspects of a token matter for memory reads vs. writes.`,
    intuition: `Three different lenses over the same token — one for searching, one for filing, one for the content itself.`,
  },
  'kda-out': {
    name: 'Output Projection',
    short: 'Final linear layer mapping the gated result back to the hidden dimension.',
    what: `A linear layer that projects the gated, normalized memory-read back to the model’s hidden dimension so it can be added to the residual stream.`,
    why: `The internal head dimensions of KDA differ from the residual stream’s dimension; this projection maps back and lets the model learn how to best inject KDA’s result into the shared representation.`,
    intuition: `The adapter plug that converts this block’s internal format back to the network’s common format.`,
  },
};

// Cross-links shown as “Related components” in the detail panel.
export const RELATED = {
  'tokenized-text': ['token-embedding', 'context-length'],
  'token-embedding': ['tokenized-text', 'depth-embedding', 'linear-output'],
  'transformer-stack': ['attn-sublayer', 'ffn-sublayer', 'w-alpha', 'layer-ratio'],
  'attn-sublayer': ['kda-mla-block', 'rmsnorm-1', 'current-group-sum'],
  'rmsnorm-1': ['rmsnorm-2', 'final-rmsnorm', 'mla-qknorm'],
  'kda-mla-block': ['kda-overview', 'mla-overview', 'layer-ratio', 'nope'],
  'nope': ['kda-mla-block', 'context-length', 'mla-overview'],
  'current-group-sum': ['updated-group-sum', 'w-alpha', 'depth-sources'],
  'ffn-sublayer': ['ffn-latentmoe', 'rmsnorm-2', 'updated-group-sum'],
  'rmsnorm-2': ['rmsnorm-1', 'final-rmsnorm'],
  'ffn-latentmoe': ['ffn-sublayer', 'transformer-stack', 'layer-ratio'],
  'updated-group-sum': ['current-group-sum', 'w-alpha', 'final-block-attnres'],
  'w-alpha': ['depth-sources', 'final-block-attnres', 'updated-group-sum'],
  'depth-sources': ['depth-embedding', 'depth-earlier-blocks', 'w-alpha', 'final-block-attnres'],
  'depth-embedding': ['token-embedding', 'depth-sources'],
  'depth-earlier-blocks': ['depth-sources', 'transformer-stack'],
  'final-block-attnres': ['depth-sources', 'w-alpha', 'final-rmsnorm'],
  'final-rmsnorm': ['rmsnorm-1', 'linear-output'],
  'linear-output': ['final-rmsnorm', 'token-embedding'],
  'context-length': ['kda-overview', 'mla-latent', 'nope'],
  'layer-ratio': ['kda-overview', 'mla-overview', 'kda-mla-block'],
  'mla-overview': ['mla-latent', 'mla-qkv', 'mla-gate', 'kda-overview'],
  'mla-latent': ['mla-overview', 'mla-qkv', 'context-length'],
  'mla-qkv': ['mla-overview', 'mla-latent', 'mla-qknorm'],
  'mla-qknorm': ['mla-qkv', 'rmsnorm-1', 'kda-l2norm'],
  'mla-gate': ['mla-overview', 'kda-gate', 'mla-out'],
  'mla-out': ['mla-overview', 'kda-out'],
  'kda-overview': ['kda-delta-rule', 'kda-alpha-beta', 'mla-overview', 'layer-ratio'],
  'kda-delta-rule': ['kda-overview', 'kda-alpha-beta', 'kda-l2norm'],
  'kda-l2norm': ['kda-delta-rule', 'mla-qknorm'],
  'kda-conv': ['kda-overview', 'kda-in'],
  'kda-silu': ['kda-conv', 'kda-in'],
  'kda-alpha-beta': ['kda-delta-rule', 'kda-gate', 'kda-overview'],
  'kda-gate': ['mla-gate', 'kda-rmsnorm', 'kda-out'],
  'kda-rmsnorm': ['kda-gate', 'rmsnorm-1'],
  'kda-in': ['mla-qkv', 'kda-conv'],
  'kda-out': ['mla-out', 'kda-overview'],
};

// Optional key formulas shown in the detail panel (plain-text math).
export const FORMULAS = {
  'rmsnorm-1': 'RMSNorm(x) = x / sqrt(mean(x²) + ε) · g',
  'rmsnorm-2': 'RMSNorm(x) = x / sqrt(mean(x²) + ε) · g',
  'final-rmsnorm': 'RMSNorm(x) = x / sqrt(mean(x²) + ε) · g',
  'kda-rmsnorm': 'RMSNorm(x) = x / sqrt(mean(x²) + ε) · g',
  'mla-overview': 'Attn(Q,K,V) = softmax(QKᵀ / √d) · V,   with K,V ≈ f(latent)',
  'mla-qkv': 'score(i,j) = Qᵢ · Kⱼ / √d   →   outᵢ = Σⱼ softmaxⱼ(score) · Vⱼ',
  'mla-gate': 'y = σ(W_g · x) ⊗ Attn(x)',
  'kda-gate': 'y = σ(W_g · x) ⊗ read(S, q)',
  'kda-delta-rule': 'Sₜ = Sₜ₋₁ · diag(αₜ) + βₜ · (vₜ − Sₜ₋₁ kₜ) kₜᵀ',
  'kda-overview': 'memory update:  Sₜ = f(Sₜ₋₁, kₜ, vₜ)     read:  oₜ = Sₜ qₜ   (O(1) per token)',
  'kda-silu': 'SiLU(x) = x · σ(x) = x / (1 + e⁻ˣ)',
  'kda-l2norm': 'q̂ = q / ‖q‖₂ ,   k̂ = k / ‖k‖₂',
  'kda-alpha-beta': 'αₜ = σ(W_α xₜ)  (forget) ,   βₜ = σ(W_β xₜ)  (write strength)',
  'ffn-latentmoe': 'y = Σₑ ∈ TopK(router(x))  gₑ(x) · FFNₑ(x)',
  'linear-output': 'p(next token) = softmax(W_out · h_final)',
  'current-group-sum': 'x ← x + Sublayer(RMSNorm(x))',
  'updated-group-sum': 'x ← x + Sublayer(RMSNorm(x))',
  'w-alpha': 'block_in = w · group_sum + Σᵢ αᵢ · depth_sourceᵢ',
};

// Forward-pass order for the guided tour.
export const TOUR_ORDER = [
  'tokenized-text',
  'token-embedding',
  'transformer-stack',
  'rmsnorm-1',
  'kda-mla-block',
  'kda-overview',
  'kda-delta-rule',
  'kda-alpha-beta',
  'mla-overview',
  'mla-latent',
  'mla-gate',
  'layer-ratio',
  'nope',
  'current-group-sum',
  'rmsnorm-2',
  'ffn-latentmoe',
  'updated-group-sum',
  'depth-sources',
  'w-alpha',
  'final-block-attnres',
  'final-rmsnorm',
  'linear-output',
  'context-length',
];
